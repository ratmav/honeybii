[project]
name = "honiipy"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["pillow", "typer"]

[project.scripts]
honiipy = "honiipy.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["source/honiipy"]
