# rename repo

feat. the port is done — make the repo honiipy.

## what changes

- rename the working dir `~/Source/honeybii` -> `~/Source/honiipy`.
- rename the github repo and update the git remote.
- publish `honiipy` to pypi (name confirmed available 2026-06).
- update any lingering `honeybii` references (docs, .ishrc comments) to honiipy.

## note

board, ish package, and `ish_kanban_*` vars are already named honiipy, so this
is mostly dir / remote / pypi plumbing.

## deliverable

`~/Source/honiipy`, github repo honiipy, `pip install honiipy` works.
